{
  "name": "Finsurgent",
  "version": "0.1.0",
  "description": "Finsurgent website",
  "keywords": "finsurgent, web, html",
  "license": "",
  "author": "PhakphumV",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "bootstrap": "^5.2.3"
  }
}
